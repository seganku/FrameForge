{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "console-login",
  "description": "Permissions for the console-login WebView window (warframe.com OAuth flow)",
  "windows": ["console-login"],
  "permissions": [
    "core:default"
  ]
}
